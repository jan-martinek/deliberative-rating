{block #title}Vytvořit heslo{/block}


{block #content}
<h1>{include #title}</h1>
{if $presenter->getParam('token') == md5($juror->password)}
	<p>Vytvořte heslo pro uživatele s e-mailem <strong>{$juror->email}</strong> (e-mail je používán coby přihlašovací jméno).</p>
	{control createPasswordForm}
{else}
	<p>Heslo tohoto uživatele již bylo nastaveno.</p>
{/if}